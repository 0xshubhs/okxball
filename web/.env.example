# ---- Agentic Fantasy Football OS / web ----

# WalletConnect project id (optional — only needed if you enable WalletConnect).
# OKX Wallet via injected provider works without this.
NEXT_PUBLIC_WALLETCONNECT_ID=

# Default chain: "testnet" (X Layer testnet, chainId 195) or "mainnet" (196)
NEXT_PUBLIC_DEFAULT_NETWORK=testnet

# Deployed contract addresses (fill after running `contracts` deploy script)
NEXT_PUBLIC_PLAYER_NFT_ADDRESS=
NEXT_PUBLIC_FANTASY_LEAGUE_ADDRESS=
NEXT_PUBLIC_SCORING_ORACLE_ADDRESS=
NEXT_PUBLIC_PAYOUT_VAULT_ADDRESS=

# Optional: Anthropic key if you wire the agent to a live LLM (server-side only).
# The included agent engine runs a deterministic heuristic by default — no key required.
ANTHROPIC_API_KEY=
